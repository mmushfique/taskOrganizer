spring:
  datasource:
    url: jdbc:mysql://localhost:3306/task_organizer
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  application:
    name: Task Organizer

  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8080

