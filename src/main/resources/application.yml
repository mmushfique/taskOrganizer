spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:task_organizer}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver

  application:
    name: Task Organizer

  jpa:
    hibernate:
      ddl-auto: update
server:
  port: {SERVER_PORT:8080}

